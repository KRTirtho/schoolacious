import { QueryFailedFilter } from "./query-failed.filter";

describe("QueryFailedFilter", () => {
    it("should be defined", () => {
        expect(new QueryFailedFilter()).toBeDefined();
    });
});
